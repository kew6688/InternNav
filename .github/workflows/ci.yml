name: PR CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [ main, master, develop ]
    paths-ignore:
      - '**.md'
      - 'docs/**'

concurrency:
  group: pr-${{ github.event.pull_request.number }}-${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: self-hosted
    if: ${{ github.event.pull_request.draft == false }} # no test on draft

    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10']
        os: [linux]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run pre-commit on diff only
        if: ${{ github.event_name == 'pull_request' }}
        shell: bash -l {0}
        run: |
          git fetch origin ${{ github.base_ref }}
          pre-commit run --from-ref origin/${{ github.base_ref }} --to-ref HEAD

      - name: Run tests
        shell: bash -l {0}
        run: |

          # dataset
          ln -sf /cpfs/user/wangyukai/mp3d_data data
          ln -sf /shared/smartbot/vln-pe/kujiale/grnavigation/data/lerobot/kujiale data/vln_pe/traj_data
          ln -sf /cpfs/user/wangyukai/kujiale_data interiornav_data

          # checkpoints
          ln -sf /cpfs/user/wangyukai/checkpoints

          # run tests
          /root/miniconda3/envs/internutopia/bin/python -c "import torch,sys;print(sys.executable);print('cuda:',torch.cuda.is_available())"
          /root/miniconda3/envs/internutopia/bin/python -m pytest -q -W ignore
